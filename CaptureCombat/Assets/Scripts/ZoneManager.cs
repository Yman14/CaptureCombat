using UnityEngine;

public class ZoneManager : MonoBehaviour
{
    public void LoadZone(string zoneName)
    {
        Debug.Log("Loading zone: " + zoneName);
        // Load a new zone map
        // Alternatively, switch to a new map panel in the same canvas
    }
}
